query GetTodos($get_todos_input: get_todos_input!) {
  get_todos(get_todos_input: $get_todos_input) {
    id
    title
    description
    completed 
    created_at
    updated_at
    completed_at
  }
}
